"use client";

import { Button } from "../Button/Button";

export function ResetAccountDataButton() {
  return (
    <Button variant="warning" isPill onClick={() => {}}>
      Reset account data
    </Button>
  );
}
